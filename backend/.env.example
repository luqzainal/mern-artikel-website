# Database (Supabase PostgreSQL)
# Get this from: Supabase Dashboard > Settings > Database > Connection String > URI
# Format: postgresql://postgres:[YOUR_PASSWORD]@db.[YOUR_PROJECT_REF].supabase.co:5432/postgres
DATABASE_URL="postgresql://islamic_user:islamic_pass@localhost:5432/islamic_articles"

# JWT (Generate a strong random 32+ character string)
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="your-jwt-secret-change-this-to-random-string-min-32-chars"
JWT_EXPIRES_IN="7d"

# Google OAuth (Get from Google Cloud Console)
# https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID="your-google-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="your-google-client-secret"
GOOGLE_CALLBACK_URL="http://localhost:3000/api/auth/google/callback"

# Frontend URL (Your public frontend domain)
FRONTEND_URL="http://localhost:3001"

# Admin URL (Your public admin domain)
ADMIN_URL="http://localhost:3002"

# Session Secret (Generate another random string)
SESSION_SECRET="your-session-secret-change-this-min-32-chars"

# Node Environment
NODE_ENV="development"

# Server
PORT=3000

# File Upload
MAX_FILE_SIZE=5242880
UPLOAD_DIR="./uploads"

# CORS Origins (Comma-separated for production)
# Example: https://mysite.com,https://admin.mysite.com
CORS_ORIGINS=""
